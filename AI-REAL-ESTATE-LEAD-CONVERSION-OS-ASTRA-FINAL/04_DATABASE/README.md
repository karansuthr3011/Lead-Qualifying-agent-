# DATABASE

Use `database-schema.sql` as the current schema extract.

Do not invent a second schema.

Live deployment still requires:
- migration test
- tenant isolation test
- follow-up state test
- actual database-role/RLS verification
